def count_in_list(lst, item):
    """
    Returns the number of occurrences of `item` in the list `lst`.

    Parameters:
        lst (list): The list in which to count occurrences.
        item (Any): The element to count.

    Returns:
        int: The count of `item` in `lst`.
    """
    return lst.count(item)
